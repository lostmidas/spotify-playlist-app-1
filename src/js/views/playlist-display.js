var React = require('react');
var R = React.DOM;

module.exports = React.createClass({
  displayName: 'Playlist',

  render: function() {
    return R.div(
      // null,
      // 'Welcome to SpinSpin'
    );
  }
});
